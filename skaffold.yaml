apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/psufoxman-gmail-com/spring-boot-web:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/psufoxman-gmail-com/spring-boot-web:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-boot-web
        chartPath: charts/spring-boot-web
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/psufoxman-gmail-com/spring-boot-web"
          image.tag: "{{.DIGEST_HEX}}"
